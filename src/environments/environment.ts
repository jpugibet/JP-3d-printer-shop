export const environment = {
    production: false,
    // Backend local (descomentalo si corres el backend local)
    // apiUrl: 'http://localhost:3000'
    
    // Backend en Railway (usa esto si el backend local no está corriendo)
    apiUrl: 'https://jp-3d-printer-shop-production.up.railway.app' // ⚠️ Reemplaza con tu URL real
};
